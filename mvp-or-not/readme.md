MVP prompt

make a terminal visualization of moving clouds, the clouds should be moving left to right on the screen, use volumetric ASCII art to display the clouds, compute the clouds in a generative manner, most importantly use the MVP style of programming, write the code in the mvp-or-not/mvp directory. Run and test the program yourself

Non-MVP prompt

make a terminal visualization of moving clouds, the clouds should be moving left to right on the screen, use volumetric ASCII art to display the clouds, compute the clouds in a generative manner,write the code in the mvp-or-not/non-mvp directory. Run and test the program yourself